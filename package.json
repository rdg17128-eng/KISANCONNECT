{
  "name": "kisanconnect",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "new_app"
  ],
  "scripts": {
    "dev": "npm --prefix new_app run dev",
    "test": "npm --prefix new_app test",
    "test:razorpay": "npm --prefix new_app run test:razorpay",
    "build": "node -e \"const fs=require('fs'),cp=require('child_process'); if(fs.existsSync('./new_app/package.json')){ cp.execSync('npm --prefix new_app install --include=dev --include=optional && npm --prefix new_app run build', {stdio:'inherit'}); } else { cp.execSync('npm run build', {stdio:'inherit'}); }\""
  },
  "dependencies": {
    "razorpay": "^2.9.8"
  }
}
