[build]
  base = "new_app"
  command = "npm install --include=dev && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=dev --include=optional"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

